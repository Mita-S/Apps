# Core app
streamlit>=1.42.0
# 0.13 removed display_toolbar and made image_data opt-in via
# return_image_data=True -- pages/collect.py targets that API.
streamlit-drawable-canvas>=0.13.0
numpy>=1.24
pillow>=10.0
scipy>=1.10
pandas>=2.0

# Google sign-in (st.login / st.user) -- see SETUP.md
Authlib>=1.3.2
# Authlib ships its httpx transport as an optional extra, but Streamlit's
# /oauth2callback route imports it -- without httpx, clicking "Sign in with
# Google" 500s with ModuleNotFoundError.
httpx>=0.27

# Google Sheets storage -- see SETUP.md. No Drive client here: samples live
# in the pixel columns, so the app never uploads a file.
gspread>=6.1.0
google-auth>=2.30.0
